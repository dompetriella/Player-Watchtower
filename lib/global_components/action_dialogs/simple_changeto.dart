import 'package:flutter/material.dart';

class SimpleChangeTo extends StatelessWidget {
  const SimpleChangeTo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
